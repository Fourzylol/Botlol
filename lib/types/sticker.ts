export enum MediaType  {
	IMAGE = "image",
	VIDEO = "video"
}
export interface MetadataSticker { 
	author?: string, 
	pack?: string,
	keepScale?: boolean, 
	removebg?: string, 
	circle?: boolean
}
